langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_hide_date
    - node.type.event
id: node.event.field_hide_date
field_name: field_hide_date
entity_type: node
bundle: event
label: 'Hide date'
description: "Every meeting must have a date, but if you don't know the dates, set one and check this box to hide it from the public"
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
