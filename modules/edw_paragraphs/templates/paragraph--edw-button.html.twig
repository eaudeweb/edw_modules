{#
/**
 * @file
 * Theme override to display the 'Button' paragraph.
 *
 * Parameters:
 * - "title" (string) (default: '')
 * - "subtitle" (string) (default: '')
 * - "level" (string) (default: 'h2') The heading level.
 * - "icon" (block) (default: '')
 * - "extra_classes" (string) (default: '')
 * - "extra_attributes" (array) (default: []): format: [
 *   {
 *     "name" (string) (default: ''),
 *     "value" (optional) (string)
 *    ...
 *   ]
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}
{# Enable library if needed. #}
{#{{ attach_library('edwt/button') }}#}

{% include '@edw_paragraphs/components/edw-button.twig' with {
  variant: type|default('primary'),
  link: link,
  label: label,
}%}
