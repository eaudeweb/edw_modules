{#
/**
 * @file
 * Theme override to display the 'Quote' paragraph.
 *
 * Parameters:
 * - "citation" (string) (default: '')
 * - "author" (string) (default: '')
 * - "image" (block) (default: '')
 * - "extra_classes" (string) (default: '')
 * - "extra_attributes" (array) (default: []): format: [
 *   {
 *     "name" (string) (default: ''),
 *     "value" (optional) (string)
 *    ...
 *   ]
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}

{% include '@edw_paragraphs/components/edw-blockquote.twig' with {
  citation: content.field_description,
  author: content.field_title
}%}
