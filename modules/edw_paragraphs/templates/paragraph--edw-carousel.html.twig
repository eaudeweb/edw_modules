{#
/**
 * @file
 * Theme override to display the 'Carousel' paragraph.
 *
 * Parameters:
 * - "items" (array) (default: ''): List
 * - "centered" (bool) (default: true) Define if banner should be centered
 * - "full_width": (bool) (default: false) Full width banner
 * - "sr_previous" (string) (default: 'Previous slides') screen reader label for previous button
 * - "sr_next" (string) (default: 'Next slides') screen reader label for next button
 * - "extra_classes" (string) (default: '')
 * - "extra_attributes" (array) (default: []): format: [
 *   {
 *     "name" (string) (default: ''),
 *     "value" (optional) (string)
 *    ...
 *   ]
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}
{# Enable library if needed. #}
{#{{ attach_library('edwt/carousel') }}#}

{% include '@edw_paragraphs/components/edw-carousel.twig' with {
  items: items,
  extra_classes: 'carousel-fade',
  full_witdh: full_width
}%}
