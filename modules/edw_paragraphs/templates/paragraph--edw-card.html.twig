{#
/**
 * @file
 * Theme override to display the 'Card' paragraph.
 *
 * Parameters:
 * - "title" (string) (default: ''): Title of the card
 * - "link" (array) (default: []): Link of the card, applied to title
 * - "media" (block) (default: ''): A media image
 * - "description" (string) (default: ''): Card description
 * - "extra_classes" (string) (default: '')
 * - "extra_attributes" (array) (default: []): format: [
 *   {
 *     "name" (string) (default: ''),
 *     "value" (optional) (string)
 *    ...
 *   ]
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}
{#{{ attach_library('edwt/card') }}#}
{#{{ attach_library('edwt/list-group') }}#}

{% include '@edw_paragraphs/components/edw-card.twig' with {
  title: content.field_title,
  link: content.field_edw_link,
  description: content.field_body,
  media: content.field_media,
  primary_meta: content.field_edw_meta
}%}
