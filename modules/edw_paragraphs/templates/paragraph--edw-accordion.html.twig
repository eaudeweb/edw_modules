{#
/**
 * @file
 * Theme override to display the 'Accordion' paragraph.
 *
 * Parameters:
 * - "items" (array) (default: []): format: [
 *    {
 *      "id": (string),
 *      title: (string),
 *      body: (string),
 *      content: (block),
 *    },
 *    ...
 *   ]
 * - "extra_classes" (string) (default: '')
 * - "extra_attributes" (array) (default: []): format: [
 *   {
 *       "name" (string) (default: ''),
 *       "value" (optional) (string)
 *     ...
 *   ],
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}
{# Enable library if needed. #}
{#{{ attach_library('edwt/collapse') }}#}
{#{{ attach_library('edwt/accordion') }}#}

{% include '@edw_paragraphs/components/edw-accordion.twig' with {
  extra_classes: 'accordion-flush'
}%}
