{#
/**
 * @file
 * Theme override to display the 'Heading' paragraph.
 *
 * Parameters:
 * - "title" (string) (default: '')
 * - "subtitle" (string) (default: '')
 * - "level" (string) (default: 'h2') The heading level.
 * - "icon" (block) (default: '')
 * - "extra_classes" (string) (default: '')
 * - "extra_attributes" (array) (default: []): format: [
 *   {
 *     "name" (string) (default: ''),
 *     "value" (optional) (string)
 *    ...
 *   ]
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}

{% include '@edw_paragraphs/components/edw-heading.twig' with {
  title: content.field_title,
  subtitle: content.field_sub_title,
  level: content.heading_level
}%}
